package com.test.core.paintshop

import org.scalatest.FunSpec
import org.scalatest.Matchers

class PaintShopTest extends FunSpec with Matchers {
/**
  describe("makeBatches method") {
    it("returns proper answer") {
      val path = getClass.getResource("/testFile.txt").getPath
      PaintFactory
        .makeBatches(path)
        .split('\n') should (contain("1 0 0 0 0") and contain("IMPOSSIBLE"))
    }
  }
  */
}
